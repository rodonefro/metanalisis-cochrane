# Copy this file to .env.local for local overrides
# Set in Cloudflare Pages dashboard for production

# Full URL of the Render.com backend — NO trailing slash
# Leave blank in development (Vite proxy handles /api → localhost:8000)
VITE_API_BASE_URL=https://metanalisis-backend.onrender.com
